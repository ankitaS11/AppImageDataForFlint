name: Test Script

on:
  push:
    branches: [ testing ]

jobs:
  test:
    name: Test FLINT build script
    runs-on: ubuntu-22.04
    
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3
        
      - name: Run Script
        run: |
          bash AppImage_FLINT_Build.sh
